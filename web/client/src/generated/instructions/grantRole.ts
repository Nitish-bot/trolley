/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TROLLEY_PROGRAM_ADDRESS } from "../programs";

export const GRANT_ROLE_DISCRIMINATOR = new Uint8Array([
  218, 234, 128, 15, 82, 33, 236, 253,
]);

export function getGrantRoleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GRANT_ROLE_DISCRIMINATOR);
}

export type GrantRoleInstruction<
  TProgram extends string = typeof TROLLEY_PROGRAM_ADDRESS,
  TAccountApplication extends string | AccountMeta<string> = string,
  TAccountRole extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountApplication extends string
        ? ReadonlyAccount<TAccountApplication>
        : TAccountApplication,
      TAccountRole extends string
        ? ReadonlyAccount<TAccountRole>
        : TAccountRole,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type GrantRoleInstructionData = { discriminator: ReadonlyUint8Array };

export type GrantRoleInstructionDataArgs = {};

export function getGrantRoleInstructionDataEncoder(): FixedSizeEncoder<GrantRoleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GRANT_ROLE_DISCRIMINATOR }),
  );
}

export function getGrantRoleInstructionDataDecoder(): FixedSizeDecoder<GrantRoleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGrantRoleInstructionDataCodec(): FixedSizeCodec<
  GrantRoleInstructionDataArgs,
  GrantRoleInstructionData
> {
  return combineCodec(
    getGrantRoleInstructionDataEncoder(),
    getGrantRoleInstructionDataDecoder(),
  );
}

export type GrantRoleInput<
  TAccountApplication extends string = string,
  TAccountRole extends string = string,
  TAccountUserAccount extends string = string,
  TAccountAuthority extends string = string,
> = {
  application: Address<TAccountApplication>;
  role: Address<TAccountRole>;
  userAccount: Address<TAccountUserAccount>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getGrantRoleInstruction<
  TAccountApplication extends string,
  TAccountRole extends string,
  TAccountUserAccount extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof TROLLEY_PROGRAM_ADDRESS,
>(
  input: GrantRoleInput<
    TAccountApplication,
    TAccountRole,
    TAccountUserAccount,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): GrantRoleInstruction<
  TProgramAddress,
  TAccountApplication,
  TAccountRole,
  TAccountUserAccount,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? TROLLEY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    application: { value: input.application ?? null, isWritable: false },
    role: { value: input.role ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("application", accounts.application),
      getAccountMeta("role", accounts.role),
      getAccountMeta("userAccount", accounts.userAccount),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getGrantRoleInstructionDataEncoder().encode({}),
    programAddress,
  } as GrantRoleInstruction<
    TProgramAddress,
    TAccountApplication,
    TAccountRole,
    TAccountUserAccount,
    TAccountAuthority
  >);
}

export type ParsedGrantRoleInstruction<
  TProgram extends string = typeof TROLLEY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    application: TAccountMetas[0];
    role: TAccountMetas[1];
    userAccount: TAccountMetas[2];
    authority: TAccountMetas[3];
  };
  data: GrantRoleInstructionData;
};

export function parseGrantRoleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGrantRoleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      application: getNextAccount(),
      role: getNextAccount(),
      userAccount: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getGrantRoleInstructionDataDecoder().decode(instruction.data),
  };
}
