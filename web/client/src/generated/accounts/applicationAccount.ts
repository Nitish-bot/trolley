/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getResourceMetaDecoder,
  getResourceMetaEncoder,
  type ResourceMeta,
  type ResourceMetaArgs,
} from "../types";

export const APPLICATION_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  222, 181, 17, 200, 212, 149, 64, 88,
]);

export function getApplicationAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPLICATION_ACCOUNT_DISCRIMINATOR,
  );
}

export type ApplicationAccount = {
  discriminator: ReadonlyUint8Array;
  /** The super-admin; immutable after init */
  authority: Address;
  /** Fixed-width name baked into the PDA seed (zero-padded to 32 bytes) */
  appName: ReadonlyUint8Array;
  /** Next free bit-index for resources (0-63) */
  resourceCount: number;
  /** Fixed-size registry of all resources registered to this app */
  resources: Array<ResourceMeta>;
  /** Next free bit-index for roles (0-63) */
  roleCount: number;
  /** Canonical PDA bump */
  bump: number;
};

export type ApplicationAccountArgs = {
  /** The super-admin; immutable after init */
  authority: Address;
  /** Fixed-width name baked into the PDA seed (zero-padded to 32 bytes) */
  appName: ReadonlyUint8Array;
  /** Next free bit-index for resources (0-63) */
  resourceCount: number;
  /** Fixed-size registry of all resources registered to this app */
  resources: Array<ResourceMetaArgs>;
  /** Next free bit-index for roles (0-63) */
  roleCount: number;
  /** Canonical PDA bump */
  bump: number;
};

/** Gets the encoder for {@link ApplicationAccountArgs} account data. */
export function getApplicationAccountEncoder(): FixedSizeEncoder<ApplicationAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["appName", fixEncoderSize(getBytesEncoder(), 32)],
      ["resourceCount", getU8Encoder()],
      ["resources", getArrayEncoder(getResourceMetaEncoder(), { size: 64 })],
      ["roleCount", getU8Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: APPLICATION_ACCOUNT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ApplicationAccount} account data. */
export function getApplicationAccountDecoder(): FixedSizeDecoder<ApplicationAccount> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["appName", fixDecoderSize(getBytesDecoder(), 32)],
    ["resourceCount", getU8Decoder()],
    ["resources", getArrayDecoder(getResourceMetaDecoder(), { size: 64 })],
    ["roleCount", getU8Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link ApplicationAccount} account data. */
export function getApplicationAccountCodec(): FixedSizeCodec<
  ApplicationAccountArgs,
  ApplicationAccount
> {
  return combineCodec(
    getApplicationAccountEncoder(),
    getApplicationAccountDecoder(),
  );
}

export function decodeApplicationAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ApplicationAccount, TAddress>;
export function decodeApplicationAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ApplicationAccount, TAddress>;
export function decodeApplicationAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<ApplicationAccount, TAddress>
  | MaybeAccount<ApplicationAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getApplicationAccountDecoder(),
  );
}

export async function fetchApplicationAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ApplicationAccount, TAddress>> {
  const maybeAccount = await fetchMaybeApplicationAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeApplicationAccount<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ApplicationAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeApplicationAccount(maybeAccount);
}

export async function fetchAllApplicationAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ApplicationAccount>[]> {
  const maybeAccounts = await fetchAllMaybeApplicationAccount(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeApplicationAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ApplicationAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeApplicationAccount(maybeAccount),
  );
}

export function getApplicationAccountSize(): number {
  return 2187;
}
